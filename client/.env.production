# Production build settings. Vite auto-loads `.env.production` during
# `vite build` (mode = 'production'), so this wins over both `.env` and
# the Dockerfile's ARG default. Keep this in sync with the public
# hostname the API is served from.
VITE_API_URL=https://api.porta137.com
