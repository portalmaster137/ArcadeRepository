rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public reads, no client writes. All mutations come from the server via
    // the Firebase Admin SDK, which bypasses these rules.
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
